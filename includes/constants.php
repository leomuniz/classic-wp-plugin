<?php
/**
 * Plugin constants.
 *
 * @package Classic_WP_Plugin
 */

namespace ClassicWPPlugin;

/**
 * Plugin assets folder.
 *
 * @since 1.0.0
 * @const string
 */
define( __NAMESPACE__ . '\ASSETS_DIR', DIR . '/assets/' );

/**
 * Plugin assets URL.
 *
 * @since 1.0.0
 * @const string
 */
define( __NAMESPACE__ . '\ASSETS_URL', URL . 'assets/' );

/**
 * Plugin CSS files URL.
 *
 * @since 1.0.0
 * @const string
 */
define( __NAMESPACE__ . '\CSS_URL', ASSETS_URL . 'css/' );

/**
 * Plugin CSS files URL.
 *
 * @since 1.0.0
 * @const string
 */
define( __NAMESPACE__ . '\VIEWS_DIR', DIR . '/src/Views/' );
